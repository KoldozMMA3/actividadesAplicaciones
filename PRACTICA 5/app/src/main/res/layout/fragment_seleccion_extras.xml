<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    tools:context=".SeleccionExtrasFragment">

    <!-- Botón para seleccionar bebida -->
    <Button
        android:id="@+id/btnBebida"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Bebida"
        android:padding="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintVertical_chainStyle="packed"
        android:layout_marginTop="32dp"/>

    <!-- Botón para seleccionar papas -->
    <Button
        android:id="@+id/btnPapas"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Papas"
        android:padding="16dp"
        app:layout_constraintTop_toBottomOf="@id/btnBebida"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintVertical_chainStyle="packed"
        android:layout_marginTop="16dp"/>

    <!-- Botón para seleccionar postre -->
    <Button
        android:id="@+id/btnPostre"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Postre"
        android:padding="16dp"
        app:layout_constraintTop_toBottomOf="@id/btnPapas"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintVertical_chainStyle="packed"
        android:layout_marginTop="16dp"/>

    <!-- Botón para continuar -->
    <Button
        android:id="@+id/btnSiguiente"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Siguiente"
        android:padding="16dp"
        app:layout_constraintTop_toBottomOf="@id/btnPostre"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintVertical_chainStyle="packed"
        android:layout_marginTop="32dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>
